apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm-telebot.fullname" . }}
  labels:
    {{- include "helm-telebot.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helm-telebot.selectorLabels" . | nindent 4 }}
